A small Windows C++ utility for locking the mouse cursor to a selected monitor.

Uses the Win32 API, mainly EnumDisplayMonitors, GetMonitorInfo, and ClipCursor.

Select your monitor then use Ctrl + Alt + L to lock/unlock and Ctrl + Alt + Q to quit.
